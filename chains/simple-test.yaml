name: "Simple Test Chain"
description: "Simple chain for testing component chaining"
version: "1.0"
engine_version: "2.0"

globals:
  test_param: "value"

steps:
  - name: normalization
    enabled: true
    params:
      target_lufs: -14
      two_pass: true

  - name: audio_benchmark
    enabled: true
    params: {}
