# Test Chain: Catalog Only
# Tests catalog generation component

name: "Test: Catalog Only"
description: "Test chain for catalog component"
version: "1.0"
engine_version: "2.0"

steps:
  - name: catalog
    enabled: true
    params: {}
