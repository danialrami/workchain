name: "Test: Content Hash"
description: "Digest the source and prove the verifier can reproduce it independently."
version: "1.0"
steps:
  - name: content_hash
    enabled: true
    params:
      algorithm: sha256
      id_prefix: "lufs-"
      id_length: 8
