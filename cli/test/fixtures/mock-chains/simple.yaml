name: "Simple Test Chain"
description: "A minimal chain for testing"
version: "1.0"
engine_version: "2.0"
steps:
  - name: normalization
    enabled: true
    params:
      target_lufs: -14
  - name: catalog
    enabled: true
    params: {}
